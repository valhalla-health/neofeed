uploads/
.thumbnail/
dogfood/
*.DS_Store
Thumbs.db
*.gs.js

# Screenshots from test/runthrough-app.cjs
test/.screenshots/

# Dev-only test deps — installed on demand, never committed (see test/README.md)
node_modules/
package-lock.json

# Wrangler local state — caches the Cloudflare account ID, never commit
.wrangler/

# Serena MCP local tool state — machine-specific, never commit
.serena/

# Graphify knowledge-graph output/cache — regenerable, not source or docs
graphify-out/

# Center Point build — dist is generated; its lockfile is tracked for npm ci
center-point/dist/
!center-point/package-lock.json

# The frontend build (tools/build.mjs) — its lockfile is tracked for npm ci.
# Its OUTPUT (compiled/, vendor/) is tracked on purpose: hosts serve git as-is.
!tools/package-lock.json
